import { tableNames } from '../table-names.mjs'

export const seed = async (knex) => {
  // Deletes ALL existing entries
  await knex(tableNames.XXXXX).del()

  // Inserts seed entries
  await knex(tableNames.XXXXX).insert([])
}
